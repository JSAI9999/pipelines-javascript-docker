apiVersion: apps/v1
kind: Deployment
metadata:
  name: railwaydeployment
  labels:
    app: railway

spec:
  replicas: 3
  selector:
    matchLabels:
      app: railway

  template:
    metadata:
      labels:
        app: railway

    spec:
      containers:
        - name: railwaycont
          image: jsaikumar9999/railimg:latest
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: railwayservice

spec:
  type: NodePort
  selector:
    app: railway
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007
